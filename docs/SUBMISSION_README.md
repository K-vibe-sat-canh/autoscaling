# 🏆 Autoscaling Analysis Project - Submission

> **Project Name:** AutoScaling Predictor & Cost Optimizer  
> **Team:** Logic / Backend / Frontend / Data / Modeler (Consolidated)  
> **Event:** DataFlow 2026: The Alchemy of Minds

---

## 📖 Introduction
This project solves the challenge of **dynamic server scaling** based on traffic prediction. It combines:
1.  **AI Models (ARIMA/LSTM)** to predict future load.
2.  **AutoScaler Logic** to decide server count (Scale Up/Down).
3.  **Industrial Dashboard** to visualize real-time metrics and cost savings.

---

## 🏗️ Project Structure
```
uibackend/
├── app.py                  # MAIN API Server
├── dashboard/              # Streamlit Frontend
├── data/                   # Generated Data (M1)
│   └── clean_data.csv      # Synthetic Log Data (~60 days)
├── models/                 # AI Model Code (M2)
├── scripts/
│   ├── generate_data.py    # Script to create data
│   └── train_models.py     # Script to train AI models
├── saved_models/           # Where trained .pkl files live
└── requirements.txt        # Dependencies
```

---

## 🚀 How to Run (Reproducible Guide)

### Step 0: Install Dependencies
```powershell
pip install -r requirements.txt
```

### Step 1: Generate Data (M1 Role)
Create the synthetic dataset simulating 2 months of traffic + "Rough" days.
```powershell
python src/data_pipeline.py
# Output: data/clean_data.csv
```

### Step 2: Train Models (M2 Role)
Train the ARIMA persistence model on the generated data.
```powershell
python src/model_trainer.py
# Output: saved_models/arima_model.pkl
```

### Step 3: Start the Backend (M3 Role)
Run the FastAPI server (Logic Layer).
```powershell
uvicorn app:app --reload
# Server running at http://localhost:8000
```

### Step 4: Launch Dashboard (M4 Role)
Run the Streamlit UI (Frontend Layer).
```powershell
streamlit run dashboard/main.py
# Browser opens at http://localhost:8501
```

---

## 📊 Results & Simulation
- Go to the **Dashboard**.
- Scroll down to **Cost Simulation**.
- Click **"Run Cost Simulation with REAL DATA"**.
- View the calculated savings (typically **50-60%** vs static deployment).

## 🎥 Deliverables
- [x] Source Code (GitHub Repo Structure)
- [x] Data Generation Scripts
- [x] Model Training Pipeline
- [x] Interactive Dashboard
- [x] Final Report (This README)

---
*Generated by Agent Antigravity representing M1, M2, M3, M4.*
